.page-header
	%h1= controller.action_name.capitalize + " user"
= form_for @user, :html => { :class => "edit_user form-horizontal", :id => "edit_user" } do |f|
	= render :partial => "form", :locals => {:f => f}
